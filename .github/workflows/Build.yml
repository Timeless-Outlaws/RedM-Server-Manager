on:
  push:

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - 
        name: Clone repository
        uses: actions/checkout@v2
      -
        name: Create .env
        run: npm install
      -
        name: Build
        run: npm run build
      - 
        name: Check
        run: ./bin/run
